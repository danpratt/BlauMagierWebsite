{
  "rules": {
    "messages": {
      "$uid": {
        ".read": false,
        ".write": true,
        ".validate": "newData.hasChildren(['name', 'email', 'message'])"
      }
    },
    "support": {
      "$uid": {
        ".read": false,
        ".write": true,
        ".validate": "newData.hasChildren(['name', 'email', 'message'])"
      }
    }
  }
}